<style>
      .container99 {
        background-color: #f8f9fa; /* Set the background color */
        display: flex; /* Use flexbox for centering */
        justify-content: center; /* Center horizontally */
        align-items: center; /* Center vertically */
        height: 100vh; /* Full height of the viewport */
        text-align: center; /* Center text */
    }
    .navbar-top-text {
        
       
        border-radius: 8px; /* Optional rounded corners */
    }


    </style>
    
    <div class="cointainer99">
    <div class="navbar-top-text">
        HomeyGo - Your Booking Platform
    </div>
    </div>
    
    <script>
    window.onscroll = function() {
      var navbar = document.querySelector('.navbar-top-text');
      if (window.scrollY > 50) {  // You can adjust the scroll threshold
        navbar.style.top = '-60px'; // Hides the div by moving it above the viewport
      } else {
        navbar.style.top = '0';  // Shows the div again when scrolled to the top
      }
    };
    </script>